<%- include('../partials/header') %>

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow">
                <div class="card-header bg-warning text-dark">
                    <h4 class="mb-0">
                        <i class="fas fa-clock me-2"></i>Account Pending Approval
                    </h4>
                </div>
                <div class="card-body text-center">
                    <div class="mb-4">
                        <i class="fas fa-user-clock fa-4x text-warning mb-3"></i>
                        <h3>Welcome, <%= user.name %>!</h3>
                        <p class="lead text-muted">Your parent account is currently pending admin approval.</p>
                    </div>
                    
                    <div class="alert alert-info">
                        <h5><i class="fas fa-info-circle me-2"></i>What happens next?</h5>
                        <ul class="list-unstyled mb-0">
                            <li><i class="fas fa-check text-success me-2"></i>Your registration has been received successfully</li>
                            <li><i class="fas fa-clock text-warning me-2"></i>An administrator will review your account</li>
                            <li><i class="fas fa-envelope text-primary me-2"></i>You'll be notified once your account is approved</li>
                            <li><i class="fas fa-sign-in-alt text-success me-2"></i>You can then log in and access your dashboard</li>
                        </ul>
                    </div>
                    
                    <div class="alert alert-warning">
                        <h6><i class="fas fa-exclamation-triangle me-2"></i>Important Information</h6>
                        <p class="mb-0">
                            While waiting for approval, you can still add your children's information. 
                            This will help speed up the approval process and ensure you have access to 
                            all relevant announcements once approved.
                        </p>
                    </div>
                    
                    <div class="d-grid gap-2 d-md-flex justify-content-md-center">
                        <a href="/students/add?welcome=true" class="btn btn-primary btn-lg me-md-2">
                            <i class="fas fa-user-plus me-2"></i>Add My Children
                        </a>
                        <a href="/auth/logout" class="btn btn-outline-secondary btn-lg">
                            <i class="fas fa-sign-out-alt me-2"></i>Logout
                        </a>
                    </div>
                    
                    <hr class="my-4">
                    
                    <div class="text-muted">
                        <small>
                            <i class="fas fa-phone me-1"></i>Need help? Contact the school administration<br>
                            <i class="fas fa-envelope me-1"></i>Email: admin@school.com
                        </small>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
.card {
    border: none;
    border-radius: 15px;
}

.card-header {
    border-radius: 15px 15px 0 0 !important;
    border-bottom: none;
}

.fa-4x {
    font-size: 4rem;
}

.alert {
    border-radius: 10px;
    border: none;
}

.btn {
    border-radius: 8px;
    padding: 12px 24px;
}

.btn-lg {
    font-size: 1.1rem;
}
</style>

 