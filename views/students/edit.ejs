<%- include('../partials/header', { 
    title: 'Edit Student - Dr. Salmin Amour Sec. School',
    breadcrumbs: [{ text: 'My Students', url: '/students/my-students' }, { text: 'Edit Student' }]
}) %>

<div class="row justify-content-center">
    <div class="col-md-8 col-lg-6">
        <div class="card">
            <div class="card-header">
                <h4 class="mb-0">
                    <i class="fas fa-user-edit me-2"></i>Edit Student
                </h4>
            </div>
            <div class="card-body">
                <form method="POST" action="/students/edit/<%= student.id %>">
                    <div class="row">
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label for="index_no" class="form-label">Student Index Number *</label>
                                <div class="input-group">
                                    <span class="input-group-text">
                                        <i class="fas fa-id-card"></i>
                                    </span>
                                    <input type="text" class="form-control" id="index_no" name="index_no" value="<%= student.index_no %>" required>
                                </div>
                                <small class="form-text text-muted">Enter the official school index number</small>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label for="grade" class="form-label">Grade *</label>
                                <div class="input-group">
                                    <span class="input-group-text">
                                        <i class="fas fa-graduation-cap"></i>
                                    </span>
                                    <select class="form-select" id="grade" name="grade" required>
                                        <option value="">Select Grade</option>
                                        <option value="Form I" <%= student.grade === 'Form I' ? 'selected' : '' %>>Form I</option>
                                        <option value="Form II" <%= student.grade === 'Form II' ? 'selected' : '' %>>Form II</option>
                                        <option value="Form III" <%= student.grade === 'Form III' ? 'selected' : '' %>>Form III</option>
                                        <option value="Form IV" <%= student.grade === 'Form IV' ? 'selected' : '' %>>Form IV</option>
                                    </select>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label for="first_name" class="form-label">First Name *</label>
                                <div class="input-group">
                                    <span class="input-group-text">
                                        <i class="fas fa-user"></i>
                                    </span>
                                    <input type="text" class="form-control" id="first_name" name="first_name" value="<%= student.first_name %>" required>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="mb-3">
                                <label for="last_name" class="form-label">Last Name *</label>
                                <div class="input-group">
                                    <span class="input-group-text">
                                        <i class="fas fa-user"></i>
                                    </span>
                                    <input type="text" class="form-control" id="last_name" name="last_name" value="<%= student.last_name %>" required>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="alert alert-info">
                        <i class="fas fa-info-circle me-2"></i>
                        <strong>Note:</strong> Changes to student information will affect grade-specific announcements and communications.
                    </div>

                    <div class="d-flex justify-content-between">
                        <a href="/students/my-students" class="btn btn-secondary">
                            <i class="fas fa-arrow-left me-2"></i>Back to Students
                        </a>
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-save me-2"></i>Update Student
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div> 